# Zbroyar Z-15 skin3 — 0-byte disclosure

`tools/arsenal-assets/source/guns/senko-v9/Zbroyar Z-15 skin3.svg` is **0 bytes** at the frozen arena tip and in every reachable git blob for this path.

- Recovery from APEX-CHAOS-ASSET-VAULT is forbidden by POST-C revision law.
- Senko v9 itch.io listing is not re-imported in this pass.
- `ZBROYAR_Z15_S3` remains a separately spawnable id that shares **Z-15 ballistics** with skin1/skin2/base, and uses the C-set staged raster fallback (not a unique SVG).
- Do not claim 24 original Senko SVG sources until this file has nonzero bytes.

Evidence: `ls -l` size 0; `git log --follow --` shows the empty blob at freeze.
